using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class CircleTransitionController : MonoBehaviour
{
	//private float sizeValue = 0.0f;
	//private Image renderer;

	void Awake()
	{
		//renderer = GetComponent<Image>();
	}

	void Update ()
	{
		// Set center point
		//renderer.material.SetFloat("_CenterX", 20f);
		//renderer.material.SetFloat("_CenterY", 0.25f);

		// Set Size
		//renderer.material.SetFloat("_Size", (Mathf.Sin(Time.realtimeSinceStartup) + 1) * 50);
	}
}
